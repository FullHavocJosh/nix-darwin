---
- name: Configure Linux base system
  hosts: all
  gather_facts: yes
  become: no

  roles:
    - { role: linux-packages, tags: ['packages'] }
    - { role: system-config, tags: ['system'] }
    - { role: gui-applications, tags: ['gui'] }