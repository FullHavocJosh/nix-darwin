---
### Development Tools ###
- name: Install development tools (Fedora/RHEL)
  dnf:
    name:
      - ansible
      - cmake
      - golang
      - neovim
      - python3-devel
      - python3-pip
      - python3-setuptools
      - rust
      - cargo
      - nodejs
      - npm
      - git
      - curl
      - wget
      - graphviz
      - make
      - gcc
      - gcc-c++
      - luarocks
    state: present
  become: yes
  when: ansible_os_family == "RedHat"
  tags: ['packages', 'development']

- name: Install development tools (Ubuntu/Debian)
  apt:
    name:
      - ansible
      - cmake
      - golang-go
      - neovim
      - python3-dev
      - python3-pip
      - python3-setuptools
      - rustc
      - cargo
      - nodejs
      - npm
      - git
      - curl
      - wget
      - graphviz
      - make
      - gcc
      - g++
      - luarocks
    state: present
    update_cache: yes
  become: yes
  when: ansible_os_family == "Debian"
  tags: ['packages', 'development']

### CLI Tools ###
- name: Install CLI tools (Fedora/RHEL)
  dnf:
    name:
      - fd-find
      - fzf
      - ripgrep
      - btop
      - fastfetch
      - tmux
      - zsh
      - tree
      - stow
      - watch
      - telnet
      - tar
      - gzip
      - unzip
      - which
      - htop
      - vim
    state: present
  become: yes
  when: ansible_os_family == "RedHat"
  tags: ['packages', 'cli']

- name: Install CLI tools (Ubuntu/Debian)
  apt:
    name:
      - fd-find
      - fzf
      - ripgrep
      - btop
      - fastfetch
      - tmux
      - zsh
      - tree
      - stow
      - bsdutils  # watch
      - telnet
      - tar
      - gzip
      - unzip
      - which
      - htop
      - vim
    state: present
    update_cache: yes
  become: yes
  when: ansible_os_family == "Debian"
  tags: ['packages', 'cli']

### Network Tools ###
- name: Install network tools (Fedora/RHEL)
  dnf:
    name:
      - openssh-clients
      - sshpass
    state: present
  become: yes
  when: ansible_os_family == "RedHat"
  tags: ['packages', 'network']

- name: Install network tools (Ubuntu/Debian)
  apt:
    name:
      - openssh-client
      - sshpass
    state: present
    update_cache: yes
  become: yes
  when: ansible_os_family == "Debian"
  tags: ['packages', 'network']

### Install Flatpak ###
- name: Install Flatpak (Fedora/RHEL)
  dnf:
    name: flatpak
    state: present
  become: yes
  when: ansible_os_family == "RedHat"
  tags: ['packages', 'gui']

- name: Install Flatpak (Ubuntu/Debian)
  apt:
    name: flatpak
    state: present
    update_cache: yes
  become: yes
  when: ansible_os_family == "Debian"
  tags: ['packages', 'gui']

- name: Add Flathub repository
  flatpak_remote:
    name: flathub
    state: present
    flatpakrepo_url: https://flathub.org/repo/flathub.flatpakrepo
  tags: ['packages', 'gui']

### Language Servers (via pip/npm) ###
- name: Install Python language server and tools
  pip:
    name:
      - python-lsp-server
      - ansible-language-server
    state: present
  tags: ['packages', 'lsp']

- name: Install Node.js language servers
  npm:
    name: "{{ item }}"
    global: yes
    state: present
  loop:
    - typescript-language-server
    - bash-language-server
    - dockerfile-language-server-nodejs
    - yaml-language-server
  tags: ['packages', 'lsp']

### Install Go tools ###
- name: Install Go language server and tools
  shell: |
    go install golang.org/x/tools/gopls@latest
    go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
  environment:
    GOPATH: "{{ ansible_env.HOME }}/go"
    PATH: "{{ ansible_env.PATH }}:{{ ansible_env.HOME }}/go/bin"
  tags: ['packages', 'go']

### Install Rust tools ###
- name: Install Rust language server
  shell: rustup component add rust-analyzer
  tags: ['packages', 'rust']

### Install additional tools via cargo ###
- name: Install cargo tools
  shell: cargo install {{ item }}
  loop:
    - eza
    - zoxide
  tags: ['packages', 'cargo']