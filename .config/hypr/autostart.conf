# Extra autostart processes
# Add your personal autostart applications here
# These will run after omarchy's default autostart apps

# Example: Start Syncthing
# exec-once = syncthing -no-browser

# Example: Start Nextcloud
# exec-once = nextcloud --background

# Example: Start custom scripts
# exec-once = ~/.config/hypr/scripts/custom-startup.sh
