# Application bindings
# Your personal application launcher bindings
# These supplement (not replace) omarchy's default bindings

# Terminal
bindd = SUPER, RETURN, Terminal, exec, alacritty

# Add your custom application bindings below
# Examples:
# bindd = SUPER SHIFT, B, Browser, exec, firefox
# bindd = SUPER SHIFT, E, Email, exec, firefox --new-window https://mail.google.com
