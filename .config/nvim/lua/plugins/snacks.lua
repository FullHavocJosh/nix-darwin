return {
  "folke/snacks.nvim",
  opts = {
    scroll = {
      enabled = false, -- Disable scrolling animations
    },
    explorer = {
      enabled = false, -- Disable Snacks explorer, use neo-tree instead
    },
  },
}
