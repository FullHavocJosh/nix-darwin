NO_UPDATE_NEEDED

The staged changes only modify the btop configuration file (switching from a custom Catppuccin theme to TTY theme, removing GPU-related settings, and hiding disk info) and add the Ollama cask to the personal macOS packages list. These are minor configuration tweaks that don't warrant updates to the main README.md, which focuses on high-level documentation and overall system architecture rather than individual package lists or application-specific settings like btop theme choices.
