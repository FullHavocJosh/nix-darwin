---
- name: Configure Fedora for work use
  hosts: all
  gather_facts: yes
  become: no

  roles:
    - { role: fedora-packages, tags: ['packages'] }
    - { role: system-config, tags: ['system'] }
    - { role: gui-applications, tags: ['gui'] }
    - { role: work-config, tags: ['work'] }